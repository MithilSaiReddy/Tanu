# Tanu package (unified — agent framework, tools, server, UI all under src/tanu/)
-e .

# Core dependencies
requests
aiohttp
sounddevice
piper-tts
pygame-ce
websocket-client

# Optional: Wake word detection
openwakeword

# Optional: Gmail
google-auth
google-auth-oauthlib
google-api-python-client

# Optional: Notifications (cross-platform)
plyer

# Optional: Discord
discord.py