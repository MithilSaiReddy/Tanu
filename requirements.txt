# Tanu package (bujji is now a git submodule — init with: git submodule update --init)
-e bujji
-e .

# Core dependencies
requests
aiohttp
sounddevice
piper-tts

# Optional: Wake word detection
openwakeword

# Optional: Gmail
google-auth
google-auth-oauthlib
google-api-python-client

# Optional: Notifications (cross-platform)
plyer

# Optional: Discord
discord.py